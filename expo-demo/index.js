import { registerRootComponent } from 'expo';
import App from './App';

// Standard Expo entry point (SDK 50+). Registers the root component so the app
// works both in Expo Go and in a native build.
registerRootComponent(App);
